// IT=  isso
// Decribe categoria de testes
describe('Authtentication', () => {
    it('should sum two numbers', () => {
        const x = 2;
        const y = 4;

        const sum = x + y;
        expect(sum).toBe(6);
    });
});
